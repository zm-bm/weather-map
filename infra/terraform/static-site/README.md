# Weather Map static site infrastructure

Placeholder for the future project-owned static site stack.

Current owner:

- shared infra repo `stacks/static-sites`

Planned owner:

- this repo's `infra/terraform/static-site`

Migrate this separately from `weather-etl` because the current static-sites
state also owns other applications.
